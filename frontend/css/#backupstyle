
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ComicVault</title>
    <style>
      body {
        margin: 0;
        font-family: "Segoe UI", sans-serif;
        background: linear-gradient(135deg, #1e1e2f, #3a3a5f);
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: start;
        min-height: 100vh;
        color: #fff;
        padding: 2rem;
      }

      h1 {
        font-size: 2.5rem;
        margin-bottom: 1rem;
        text-align: center;
      }

      .app-container {
        backdrop-filter: blur(12px);
        background: rgba(255, 255, 255, 0.08);
        border-radius: 20px;
        padding: 2rem;
        max-width: 700px;
        width: 100%;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.4);
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }

      .input-group,
      .comic-list {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
      }
<button onclick="location.href='collection.html'">View My Collection</button>

      input,
      button {
        padding: 0.8rem;
        font-size 1rem;
        border{none: 
        border-radius: 10px;
        outline: none;
      }

      button {
        background: #4fc3f7;
        color: #000;
        font-weight: bold;
        cursor: pointer;
        transition: background 0.3s ease;
      }

      button:hover {
        background: #03a9f4;
      }

      .comic-item {
        background: rgba(255, 255, 255, 0.05);
        padding: 1rem;
        border-radius: 10px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-size: 1.1rem;
      }

      .total-value {
        font-size: 1.3rem;
        font-weight: bold;
        margin-top: 1rem;
        text-align: center;
      }

      .footer {
        margin-top: 2rem;
        text-align: center;
        color: rgba(255, 255, 255, 0.4);
      }

      .preview {
        max-height: 200px;
        margin-top: 10px;
        border-radius: 10px;
      }

      @media (max-width: 600px) {
        h1 {
          font-size: 2rem;
        }
      }
    </style>

